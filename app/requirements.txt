# =============================================================================
# requirements.txt
# -----------------------------------------------------------------------------
# Python package dependencies for the Linux & Docker Lab application.
#
# Install locally:   pip install -r requirements.txt
# Inside Docker:     handled automatically by the Dockerfile RUN step
#
# Pinning exact versions ensures reproducible builds across all environments.
# To update: pip install --upgrade <package>, then pin the new version here.
# =============================================================================

# --- Testing -----------------------------------------------------------------
pytest==7.4.3             # Test framework — used in CI and locally

# --- Future extensions (commented out — uncomment as needed) -----------------
# requests==2.31.0        # HTTP client — useful for testing API calls
# flask==3.0.0            # Lightweight web framework
# python-dotenv==1.0.0    # Load .env files as environment variables
